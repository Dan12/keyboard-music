interface SongStruct {
  name: string;
  bpms: number[][];
  files: string[];
  keyboard_type: string;
  container_settings: [number, string[][], boolean][][];
  linked_areas: number[][][];
  colors: any;
}
